JDK早期版本 重量级 锁, 每次需要找操作系统申请锁

sync(Object) Object 不能用String,Integer,Long 等基础数据类型
第一个线程第一次访问的时候, 在Object头的Markword 上记录这个线程ID (称为 偏向锁) 
如果有别的线程争用这把锁, 锁升级为 自旋锁 ,默认十次 (执行时间短,线程数量少,用自旋锁)
十次之后,升级为重量级锁


优化:
1. 同步代码块中的语句越少越好
2. 用对象当锁的时候,用final修饰对象, 因为对象可能发生改变